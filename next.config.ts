// next.config.ts
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Add any specific Next.js config you need here, like reactStrictMode
  // reactStrictMode: true,
};

export default nextConfig; // Export directly